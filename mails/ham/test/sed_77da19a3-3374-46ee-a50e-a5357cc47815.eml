To: to@example.com
From: from@example.com
Subject:

folks i'd appreciate if someone could straighten me out on a few concepts which are described a bit ambiguously in the docs escapenumber data frame refan pescapenumber 'a data frame is a list of variables of the same length with unique row names given class data frame ' i probably don't need to point out how opaque that is anyhow key question some places in the docs seem pretty firm that a data frame is basically a escapenumber d array with a named rows and b columns whose items within a column be of uniform data type elsewhere it seems like a data frame can be a collection of arbitrary variables escapenumber environment refman pescapenumber environments consist of a frame or collection of named objects and a pointer to an enclosing environment is the or here explaining parenthetically that a frame is a collection of named objects or is separating too alternative structures for an environment if the former does this imply that a frame can contain arbitrary variables and pointer is that a type of thing in r escapenumber r search path attach the r search path appears to hold the list of collections of data my term that can be accessed by a users' commands refman pescapenumber tells that search path can hold items that are data frame list environment or r data file on disk yet r intro pescapenumber describes attach as taking a directory name argument what is the concept directory in this context thanks graham r help stat math ethz ch mailing list https stat ethz ch mailman listinfo r help please do read the posting guide http www r project org posting guide html and provide commented minimal self contained reproducible code 