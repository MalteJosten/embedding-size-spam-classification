To: to@example.com
From: from@example.com
Subject:

didn't look at the implementation so that also makes the if ret flock escapenumber code that will never get executed might be better to just leave the comment here and just void smb vfs kernel flock fsp fsp fh fd share access and remove the error check code jeremy allison wrote on mon may escapenumber escapenumber at escapenumber escapenumber escapenumberam escapenumber herb lewis wrote in smbd open c in the function open file ntcreate we have the following comment and code which contradict note that we ignore failure for the following it is basically a hack for nfs and nfs will never set one of these only read them nobody but samba can ever set a deny mode and we have already checked our more authoritative locking database for permission to set this deny mode if the kernel refuses the operations then the kernel is wrong note that gpfs supports it as well jmcd ret flock smb vfs kernel flock fsp fsp fh fd share access if ret flock escapenumber talloc free lck fd close conn fsp file free fsp return nt status sharing violation but if you look at the implementation it goes static int vfswrap kernel flock vfs handle struct handle files struct fsp int fd uintescapenumber share mode start profile syscall kernel flock kernel flock fd share mode end profile syscall kernel flock return escapenumber always returns ok confusing comment needs to be moved to modules vfs default c i think 