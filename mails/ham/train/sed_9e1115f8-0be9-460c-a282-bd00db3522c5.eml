To: to@example.com
From: from@example.com
Subject:

thanks rob and chas on escapenumber escapenumber escapenumber rob dixon wrote sharan basappa wrote hi all i have some background working with scanners built from flex and i have used lookahead capability of flex many a times but i dont understand the meaning of zero in zero lookahead match rule i e pattern for example to capture overlapping escapenumber digit patterns from string str escapenumber i use the regex store str m \\d\\d\\d g so here the regex engine actually looks ahead by chars digits as far as lookahead expressions are concerned perl functions identically to flex it is called zero width lookahead because it matches a zero width position in the string instead of a sequence of characters if i write 'escapenumber' \\d\\d\\d then 'escapenumber' will be captured as the first three characters were consumed by the preceding pattern however if i write 'escapenumber' \\d\\d\\d then 'escapenumber' will be captured instead because the lookahead pattern has zero width the other question i have is how does regex engine decide that it has to move further its scanner by escapenumber character everytime since i get output escapenumber escapenumber escapenumber escapenumber when i run this script the engine moves as far through your target string as it needs to to find a new match if i write 'escapelong' \\d g then the engine will find a match at only every second character and if i use a much simpler zero width match just 'abcdef' g then the regex will match seven times at the beginning and end and between every pair of characters so the more complex zero width match you have written will match at all of the those places as long as there are three digits following hth rob 